# Environment variables declared in this file are NOT automatically loaded by Prisma.
# Please add `import "dotenv/config";` to your `prisma.config.ts` file, or use the Prisma CLI with Bun
# to load environment variables from .env files: https://pris.ly/prisma-config-env-vars.

# Prisma supports the native connection string format for PostgreSQL, MySQL, SQLite, SQL Server, MongoDB and CockroachDB.
# See the documentation for all the connection string options: https://pris.ly/d/connection-strings

# The following `prisma+postgres` URL is similar to the URL produced by running a local Prisma Postgres
# server with the `prisma dev` CLI command, when not choosing any non-default ports or settings. The API key, unlike the
# one found in a remote Prisma Postgres URL, does not contain any sensitive information.

DATABASE_URL="postgresql://postgres:saiashish2908@localhost:5432/no_smoking_db?schema=public"

EMAIL_HOST=smtp.gmail.com
EMAIL_PORT=587
EMAIL_USER=saiashishmishra290806@gmail.com
EMAIL_PASS=rmxcfhzsxqoiolxl 

JWT_SECRET=shaastraisgood2027

ADMIN_EMAIL = rockstarsanty29@gmail.com
ADMIN_PASSWORD = admin123


RAZORPAY_KEY_ID=rzp_test_ShL3nIFOUJhFWt
RAZORPAY_SECRET=urhn420ftuR9W0vasnmpEajJ